{
  "name": "ai-developer-workspace-frontend",
  "private": true,
  "//": "package.json — minimal dependency set for the dashboard: React + TypeScript + Tailwind, plus a fetch wrapper for talking to the FastAPI backend.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.0",
    "react-dom": "^18.3.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.2.0",
    "@vitejs/plugin-react": "^4.2.0",
    "tailwindcss": "^3.4.0"
  }
}
